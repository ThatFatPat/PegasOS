/**
 * @defgroup psl PegasOS Support Library
 * Header-only supplementary library used in places where the c++ standard
 * library is not available (the kernel and early user-mode binaries). Due to
 * the constrained environments in which it is used, this library must be
 * header-only and can only depend on the freestanding C headers.
 */
